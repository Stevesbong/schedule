extends layout.pug

block content
    .password-container
        h1 Schedule Access
            a.button.cancel-button( href="/schedules") Cancel
        form.admin-passowrd(action="/schedules/admin", method="post")
            input(name="admin", type="password", id="admin", placeholder="Enter Admin Password")
            input(type="submit", value="Submit", id="submit-admin")